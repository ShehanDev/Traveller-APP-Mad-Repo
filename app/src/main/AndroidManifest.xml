<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.traveller">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Traveller">
        <activity
            android:name=".common.HomePage"
            android:label="@string/title_activity_home_page"
            android:theme="@style/Theme.Traveller.NoActionBar"></activity>
        <activity android:name=".user.TripDetails" />
        <activity android:name=".user.UserDashboard" />
        <activity android:name=".common.Register" />
        <activity android:name=".common.Login" />
        <activity android:name=".user.MyTrips" />
        <activity android:name=".user.Addtrips" />
        <activity android:name=".common.OnBoarding" />
        <activity android:name=".user.uerDashBoard" />
        <activity android:name=".common.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>